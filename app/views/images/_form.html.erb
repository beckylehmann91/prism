<%= image_tag @image.filename %>
<%= form_for(@image) do |f| %>
  <div class="field">
    <%= f.label :filename %><br>
    <%= f.file_field :filename %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
